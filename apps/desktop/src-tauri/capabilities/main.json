{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The LongClaw desktop webview may use typed IPC/events and open one native folder picker. It has no frontend filesystem, shell, network, or process capability: where a command reaches the disk or the OS — reading a ticket, or handing one to the editor via open_ticket_file — the webview names a ticket key and Rust resolves the path inside the open project.",
  "windows": ["main"],
  "platforms": ["macOS"],
  "permissions": ["core:default", "core:event:default", "dialog:allow-open"]
}
