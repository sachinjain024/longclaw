{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The spike webview may use typed IPC/events and open one native folder picker. It has no frontend filesystem, shell, network, or process capability.",
  "windows": ["main"],
  "platforms": ["macOS"],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:allow-open"
  ]
}
